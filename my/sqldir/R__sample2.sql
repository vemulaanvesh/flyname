

IF NOT EXISTS (SELECT * FROM flyway1.dbo.anvesh)
    CREATE TABLE anvesh (
     id INT NOT NULL,
     name varchar(45));

IF NOT EXISTS (SELECT * FROM flyway1.dbo.lenin)
    CREATE TABLE anvesh (
     id INT NOT NULL,
     name varchar(45));

